{
  "name": "@ai-idp/types",
  "version": "1.0.0",
  "description": "Shared TypeScript types for Multi-Agent IDP architecture",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && echo '@ai-idp/types Build complete'",
    "dev": "tsc --watch",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}